# راه‌اندازی CMS و فروشگاه Maktumbook

ظاهر پروژه حفظ شده و داده‌های نمایشی با یک لایه آماده اتصال به Supabase جایگزین شده‌اند.

## ۱. ساخت دیتابیس

1. یک پروژه در Supabase بسازید.
2. فایل `supabase/schema.sql` را در SQL Editor اجرا کنید.
3. در Authentication، ورود با Email را فعال نگه دارید.

## ۲. تنظیم متغیرها

فایل `.env.example` را با نام `.env` کپی و سه مقدار آن را وارد کنید:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_TELEGRAM_USERNAME` (بدون @)

کلید Service Role را هرگز در این پروژه یا مرورگر قرار ندهید.

## ۳. ساخت مدیر

ابتدا از صفحه ورود یک حساب بسازید. سپس در Supabase، جدول `profiles` را باز کنید و مقدار `role` همان حساب را از `user` به `admin` تغییر دهید.

## ۴. ورود محتوا

به `/admin/books` بروید. عنوان، نویسنده، قیمت، توضیحات و تصاویر صفحات را وارد کنید. صفحات باید به ترتیب و ترجیحاً WebP/JPEG بهینه‌شده باشند؛ فایل PDF خام در اختیار مرورگر قرار نمی‌گیرد.

در `/admin/access` درخواست‌های خرید دیده می‌شوند. بعد از بررسی رسید در تلگرام، «تأیید و اعطای دسترسی» را بزنید.

## محدودیت امنیتی واقعی

وب نمی‌تواند اسکرین‌شات سیستم‌عامل یا عکس گرفتن با گوشی دیگر را صددرصد متوقف کند. این نسخه لینک دانلود مستقیم را نمی‌دهد، فایل‌ها را در فضای خصوصی نگه می‌دارد، URL دو دقیقه‌ای می‌سازد، چاپ و انتخاب/کشیدن محتوا را محدود می‌کند و ایمیل کاربر را به‌صورت واترمارک نشان می‌دهد. برای بازدارندگی بیشتر، تصاویر صفحات را با واترمارک سروری اختصاصی هر کاربر تولید کنید.
